Put a **public**, shareable corpus here for the published eval (never customer data).
Suggested: a set of open-license contracts/policies, or public-domain manuals, ~50–200 pages.
Then write `eval/questions.jsonl` (see `questions.example.jsonl`) with 30+ questions whose answers you've verified by hand.
